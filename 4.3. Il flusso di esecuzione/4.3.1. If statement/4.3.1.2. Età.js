/* 
Scrivi un programma che chieda all'utente di inserire la propria età e che stampi:
- "Bambino" se l'età è inferiore a 12 anni
- "Adolescente" se l'età è compresa tra 12 e 18 anni
- "Adulto" altrimenti
*/

function setup() {
    // Chiedi all'utente di inserire la propria età
    var eta = prompt("Inserisci la tua età:");
    // Converti l'input in un numero
    eta = parseInt(eta); 

    // continua tu
    
}
